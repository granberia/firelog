---
title: 지식 베이스를 하나의 프롬프트로 합치는 유틸리티 개발
date: 2025-12-01 12:47:22 +0900
categories: [개발]
tags: [개발, AI 채팅, TRPG]
---


지식 베이스를 LLM에 주입하기 위해 로컬 환경에 작성된 파일들을 하나의 프롬프트로 합치는 유틸리티를 만들자.

이번에도 LLM을 사용해서 빠르게 스크립트를 완성할 수 있을까?

### 프롬프트

~~~
당신은 nodejs로 아래의 스펙을 만족하는 코드를 작성해야 합니다.

- inquirer를 사용해서 experiment 폴더 내부에서 하나의 하위 폴더를 선택해야 합니다.
- name이라는 이름을 가진 폴더가 선택되었다면 아래 디렉터리에 있는 모든 파일들을 합쳐서 name 폴더 내부에 KB.txt 로 출력해야 합니다.
  - experiment/{name}/KB/episodes
  - experiment/{name}/KB/nations
  - experiment/{name}/KB/locations
  - experiment/{name}/KB/religions
  - experiment/{name}/KB/races
  - experiment/{name}/KB/characters
  - experiment/{name}/KB/lore
- KB.txt 의 가장 위 줄에는 `**지식 베이스(KB) 내용:**` 이라는 텍스트와 빈 줄 하나가 포함되어야 합니다. 그 아래로 각 파일들의 내용이 순서대로 포함되어야 합니다.
- 각 파일의 내용을 추가하기 시작할 때 아래 예시와 같이 ---와 파일 경로가 파일의 내용보다 앞에 포함되어 있어야 합니다.
```
---

episodes/episode_0.md
```
~~~



### 제미니 3 Pro의 결과물

<details markdown="1">
<summary>접기/펴기</summary>

```
import fs from 'fs/promises';
import path from 'path';
import inquirer from 'inquirer';

// 기본 디렉터리 설정
const EXPERIMENT_DIR = './experiment';

// 합쳐야 할 하위 폴더 목록
const TARGET_SUBDIRS = [
  'episodes',
  'nations',
  'locations',
  'religions',
  'races',
  'characters',
  'lore',
];

async function main() {
  try {
    // 1. experiment 폴더가 있는지 확인
    try {
      await fs.access(EXPERIMENT_DIR);
    } catch (error) {
      console.error(`❌ 오류: '${EXPERIMENT_DIR}' 디렉터리를 찾을 수 없습니다.`);
      return;
    }

    // 2. experiment 폴더 내부의 하위 폴더 목록 가져오기
    const dirents = await fs.readdir(EXPERIMENT_DIR, { withFileTypes: true });
    const experimentFolders = dirents
      .filter((dirent) => dirent.isDirectory())
      .map((dirent) => dirent.name);

    if (experimentFolders.length === 0) {
      console.log(`❌ '${EXPERIMENT_DIR}' 내부에 선택할 폴더가 없습니다.`);
      return;
    }

    // 3. inquirer를 사용하여 폴더 선택
    const answers = await inquirer.prompt([
      {
        type: 'list',
        name: 'selectedFolder',
        message: '작업할 폴더를 선택해주세요:',
        choices: experimentFolders,
      },
    ]);

    const selectedName = answers.selectedFolder;
    const kbBasePath = path.join(EXPERIMENT_DIR, selectedName, 'KB');
    
    // KB.txt 파일의 초기 내용
    let outputContent = '**지식 베이스(KB) 내용:**\n\n';

    console.log(`\n📂 '${selectedName}' 폴더 처리를 시작합니다...`);

    // 4. 각 타겟 폴더 순회하며 내용 수집
    for (const subDirName of TARGET_SUBDIRS) {
      const currentDirPath = path.join(kbBasePath, subDirName);

      // 해당 폴더가 존재하는지 확인
      try {
        await fs.access(currentDirPath);
      } catch (e) {
        // 폴더가 없으면 건너뜀
        continue;
      }

      // 폴더 내의 파일 목록 읽기
      const files = await fs.readdir(currentDirPath, { withFileTypes: true });

      for (const file of files) {
        if (file.isFile()) {
          const filePath = path.join(currentDirPath, file.name);
          
          // 파일 내용 읽기
          const fileContent = await fs.readFile(filePath, 'utf-8');
          
          // 경로 표시용 (OS 상관없이 슬래시(/) 사용하도록 변환)
          const displayPath = `${subDirName}/${file.name}`;

          // 형식에 맞춰 내용 추가
          // ---
          // <빈줄>
          // 경로
          // <빈줄>
          // 내용
          outputContent += `---\n\n${displayPath}\n\n${fileContent}\n\n`;
        }
      }
    }

    // 5. KB.txt 파일로 저장 (experiment/{name}/KB.txt)
    // 요구사항: name 폴더 내부에 KB.txt로 출력
    const outputPath = path.join(EXPERIMENT_DIR, selectedName, 'KB.txt');
    
    await fs.writeFile(outputPath, outputContent.trim(), 'utf-8');

    console.log(`✅ 성공적으로 병합되었습니다!`);
    console.log(`📄 출력 파일: ${outputPath}`);

  } catch (error) {
    console.error('❌ 에러 발생:', error);
  }
}

main();
```

</details>

### GPT-5의 결과물

<details markdown="1">
<summary>접기/펴기</summary>

```
import fs from "fs";
import path from "path";
import inquirer from "inquirer";

const baseDir = path.resolve("experiment");

async function main() {
  // experiment 폴더 내의 디렉터리 목록 가져오기
  const folders = fs.readdirSync(baseDir).filter(file =>
    fs.statSync(path.join(baseDir, file)).isDirectory()
  );

  if (folders.length === 0) {
    console.error("experiment 폴더 아래에 하위 폴더가 없습니다.");
    return;
  }

  // inquirer로 하위 폴더 선택
  const { name } = await inquirer.prompt([
    {
      type: "list",
      name: "name",
      message: "대상 폴더를 선택하세요:",
      choices: folders,
    },
  ]);

  const targetKBDir = path.join(baseDir, name, "KB");
  const outputFilePath = path.join(baseDir, name, "KB.txt");

  const subDirs = [
    "episodes",
    "nations",
    "locations",
    "religions",
    "races",
    "characters",
    "lore",
  ];

  let output = `**지식 베이스(KB) 내용:**\n\n`;

  for (const subDir of subDirs) {
    const fullDir = path.join(targetKBDir, subDir);

    if (!fs.existsSync(fullDir)) continue;

    const files = fs.readdirSync(fullDir).filter(file =>
      fs.statSync(path.join(fullDir, file)).isFile()
    );

    for (const file of files) {
      const filePath = path.join(fullDir, file);
      const content = fs.readFileSync(filePath, "utf-8");

      output += `---\n\n${subDir}/${file}\n\n${content}\n\n`;
    }
  }

  fs.writeFileSync(outputFilePath, output, "utf-8");
  console.log(`KB.txt 생성 완료: ${outputFilePath}`);
}

main().catch(console.error);
```

</details>
<br>

둘 다 사용해봤는데 GPT가 작성해준 코드가 짧아서 GPT의 코드를 쓰기로 했다.

굉장하군, 요즘 AI 코딩...
